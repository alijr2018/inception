# sudo mariadb-secure-installation
# 
# sudo systemctl status mariadb
# 
# sudo systemctl start mariadb
# 
# mariadb -u root -p
# 
# /etc/my.cnf /etc/mysql/my.cnf ~/.my.cnf 
# 

# mkdir -p /run/mysqld
# chown mysql:mysql /run/mysqld

set -e

echo "Start MariaDB init"

if [ ! -d "/var/lib/mysql/mysql" ]; then
    mysql_install_db --user=mysql --datadir=/var/lib/mysql > /dev/null
fi

mysql --socket=/run/mysqld/mysqld.sock -u root << EOF
ALTER USER 'root'@'localhost' IDENTIFIED BY '${MARIADB_PASSWOD}';
CREATE DATABASE IF NO EXSITS ${MARIADB_DATABASE};
CREATE USER IF NOT EXSITS '${MARIADB_USER}'@'%' IDENTIFIED BY '${MARIADB_PASSWOD};
GRANT ALL PRIVILEGES ON ${MARIADB_DATABASE}.* TO '${MARIADB_DATABASE}'@'%';
FLUSH PRIVILEGES;
EOF

exec mysqld --user=mysql --datadir=/var/lib/mysql --socket=/run/mysqld/mysql.sock